import GalleryPage from "@/components/gallery/GalleryPage";

export const metadata = {
  title: "Gallery | Manash Hada",
  description:
    "Photo gallery for Manash Hada with keyboard and click navigation across project, event, and team images.",
};

export default function GalleryRoute() {
  return <GalleryPage />;
}
